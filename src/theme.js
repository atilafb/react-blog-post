const colors = {
  black: '#000000',
  white: '#FFFFFF',
  grey: {
    100: '#F5F5F5',
    500: '#9E9E9E',
    700: '#616161'
  }
}

const space = ['0px', '4px', '8px', '16px', '32px', '64px', '128px', '256px']

const fonts = {
  body: 'Roboto'
}

const fontSizes = ['0px', '8px', '10px', '12px', '14px', '16px', '18px', '20px', '24px', '28px', '32px', '36px', '40px']

const fontWeights = [0, 300, 400, 500, 600]

const radii = ['0px', '2px', '4px']

const zIndices = []

const sizes = []

const theme = {
  colors,
  space,
  fonts,
  fontSizes,
  fontWeights,
  radii,
  zIndices,
  sizes
}

export default theme
